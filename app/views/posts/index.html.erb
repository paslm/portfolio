<%  @posts.each do |post|%>
    <h1><%= post.name %></h1>
    <p><%= post.description %></p>
    <%= link_to "See post", post_path(post) %>
    <%= link_to "update post", post_path(post), method: :patch %>
    <%= link_to "Delete  post", post_path(post), method: :delete %>
    <p>--------------------------------------</p>
<% end %>

<%= link_to "Create New Post", new_post_path %>